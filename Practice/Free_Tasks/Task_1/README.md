# Task: Count Delivery Points

## Condition
There is a list of delivery addresses for today.  
You need to determine how many orders must be delivered in total.

## Solution
We created a list of delivery addresses and used the `len()` function to count how many orders need to be delivered.  
The program prints the total number of delivery points.  

**Tools used:** list, `len()`, print.