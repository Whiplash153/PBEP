# Task: Top Customer Finder

Write a function that:
1. Takes a dictionary where keys are customer names and values are lists of their purchase amounts;
2. Returns the name of the customer who spent the most in total;
3. Returns `None` if all purchase lists are empty.

Tools used: functions, conditionals, loops, dict methods (`.items()`), and `sum()`.