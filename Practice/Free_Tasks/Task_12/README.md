# Task: Orders Sum Function

Write a function that:
1. Takes a list of orders (each order is a tuple or list with a name and a price);
2. Returns the total sum of all orders;
3. Returns `None` if the list is empty.

Tools used: functions, conditionals, loops, None check.