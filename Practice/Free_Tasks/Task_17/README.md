# Task: Safe Divide

Create a function that:
- requests two numbers from the user;
- handles invalid input and division by zero;
- returns the result of the division or `None` if an error occurs;
- always prints a completion message in a `finally` block.

Tools used: functions, exception handling (`try/except/finally`), value returning, conditional checks, number rounding.